foo:
  name: foo

bar:
  name: bar

with_domain:
  name: domain
  subdomain: domain
  domain: domain.com

abc:
  name: abc
  users: john, bob # habtm relation

def:
  name: def
  users: john, alice # habtm relation